name: acsl_mock_server
description: In-memory stub backend for the ACSL Campaign app — local dev + Maestro E2E.
publish_to: "none"
version: 0.1.0

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4

dev_dependencies:
  lints: ^4.0.0
